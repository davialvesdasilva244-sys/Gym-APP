# Sistema de Gestão de Academia (Python + SQLite)

Este projeto é um sistema interativo de gerenciamento de academia
desenvolvido em Python utilizando SQLite, totalmente compatível com
ambientes como **OneCompiler**, **Replit** ou qualquer instalação local
de Python.

## ✅ Funcionalidades

-   Cadastro e login de usuários
-   Listagem de academias
-   Sistema de check-in
-   Histórico de check-ins
-   Operações SQL completas:
    -   INSERT INTO VALUES
    -   INSERT INTO SELECT
    -   SELECT com JOIN
    -   ORDER BY
    -   DELETE com WHERE
-   Banco de dados SQLite em memória (não cria arquivos)

## 🚀 Tecnologias Utilizadas

-   **Python 3**
-   **SQLite (em memória)**
-   Apenas bibliotecas padrão (sem dependências externas)

## 📌 Destaques SQL

O projeto segue normas SQL e inclui: - `INSERT INTO ... SELECT` - `JOIN`
com `SELECT` - `ORDER BY` - `DELETE` com `WHERE` - Estrutura SQL limpa e
organizada para fins acadêmicos

## ▶️ Como Executar

1.  Acesse OneCompiler.com → Python 3\
2.  Copie o código completo do projeto\
3.  Execute o programa\
4.  Utilize o menu interativo para:
    -   Registrar-se
    -   Fazer login
    -   Realizar check-in
    -   Ver lista de academias
    -   Ver histórico


